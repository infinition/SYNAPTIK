# Dependencies (if a build step is added later)
node_modules/

# Build / bundler output
dist/
build/
.cache/
.parcel-cache/

# Editor / IDE
.vscode/
.idea/
*.iml
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Environment
.env
.env.local

# Arduino compilation artefacts (generated by Arduino IDE in the sketch folder)
Arduino_code/**/build/
Arduino_code/**/*.elf
Arduino_code/**/*.hex
Arduino_code/**/*.map

# User-uploaded assets (OBJ meshes, etc. — large, local to each setup)
uploads/
*.obj
*.mtl

# Project save files generated at runtime (JSON sequences, CSV exports)
# Uncomment if you do not want to track example saves:
# saves/
# *.csv
